/**
 * Implementations of the individual Sprig functions, grouped by category (strings, lists,
 * collections, dicts, math, logic, crypto, encoding, date, semver, network, path, and
 * reflection).
 *
 * <p>
 * <strong>Internal implementation detail.</strong> These classes are {@code public} only
 * because of Java package boundaries; they are not part of the stable public API and may
 * change without notice between releases. The functions are registered with the engine
 * via {@link org.alexmond.gotmpl4j.sprig.SprigFunctionProvider}.
 */
package org.alexmond.gotmpl4j.sprig.functions;
