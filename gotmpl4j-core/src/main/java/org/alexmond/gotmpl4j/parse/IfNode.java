package org.alexmond.gotmpl4j.parse;

/**
 * An {@code {{if}}} action: a {@link BranchNode} whose pipeline is evaluated for truth to
 * choose between the {@code if} body and the optional {@code else} body.
 */
public class IfNode extends BranchNode {

}
