package org.alexmond.gotmpl4j.parse;

/**
 * A {@code {{range}}} action: a {@link BranchNode} that iterates over the value produced
 * by its pipeline, rendering the body once per element (or the {@code else} body when
 * empty).
 */
public class RangeNode extends BranchNode {

}
